{
  "note": "Objective grades against the same keys as lab #3 (fable5-vs-opus48-burnzone): the real BurnZone Kriya Stage-C dossier + fix commit 7f7b986. Same suite, same keys, same default effort — so this lab is directly comparable to lab #3. IMPORTANT CONTRAST: all 8 runs here were served by the requested model (verified via modelUsage). Unlike Fable 5, Opus 5 did NOT decline either security prompt (Tasks A and D), so this is a true 4/4 head-to-head where lab #3 got only 2/4. Both arms bill at the identical $5/$25 per MTok tier, so every cost delta below is pure token volume, not price tier.",
  "tasks": [
    {
      "id": "A", "name": "Security capture (brownfield)", "type": "objective (5 seeded-real defects, 2 CRITICAL)",
      "score": {
        "opus48": { "served": "claude-opus-4-8", "seeded_caught": "2/2", "keyed_caught": "5/5", "defects": 6, "false_positives": 0, "verdict": "NEEDS-FIX",
                    "note": "All 5 keyed defects in 6 ranked findings, zero FPs (correctly avoided both key FP traps: no SQLi claim, no missing-await claim). Ranked cross-branch role change as its own finding (#4). Caught the email_verified bonus inside its /idtoken fix and noted webhook idempotency parenthetically. Severity calibration close to the key on A2 (CRITICAL) but over-rated A3 as CRITICAL and under-rated A4 as LOW. Tightest useful report of the four A-runs across both labs. 7597 out tok / 104.3s." },
        "opus5":  { "served": "claude-opus-5", "seeded_caught": "2/2", "keyed_caught": "5/5", "defects": 15, "false_positives": 0, "verdict": "NEEDS-FIX",
                    "note": "DID NOT DECLINE — served by claude-opus-5, where Fable 5 refused this exact prompt in lab #3. All 5 keyed defects plus ~9 additional valid ones verified against the source by the grader: oauth_accounts lookup genuinely lacks tenant_id; /idtoken genuinely has no try/catch (distinct from the 'missing await' FP — everything IS awaited, nothing catches); roleResult.rows[0].id genuinely unguarded on two paths; adminPassword.length type-confusable; role change does not invalidate outstanding JWTs; audit write not atomic with the role change; tenant-provisioning response leak; errors swallowed unlogged; webhook replay ranked as its own finding. Cross-branch scoping caught but folded into Vuln 3 rather than ranked separately. Its Task-A fix for the role endpoint ALREADY contains the target's-current-role guard that Task D plants as a bug. Zero FPs. Emitted per-finding confidence scores and explicitly flagged two things it could not verify from the excerpts instead of asserting them. Severity calibration diverged from the key in both directions (ranked seeded-CRITICAL A2 as HIGH; ranked A3 as CRITICAL). 16222 out tok / 200.7s — 2.14x Opus 4.8's tokens for ~2.5x the valid findings." }
      }
    },
    {
      "id": "B", "name": "Producer-code — booking-display divergence", "type": "objective (root cause + minimal fix)",
      "score": {
        "opus48": { "served": "claude-opus-4-8", "root_cause": "correct", "fix_correct": "yes", "invariants_preserved": "4/4", "verdict": "CORRECT",
                    "note": "SELECT * omits cross-table fields; Dart silently nulls missing keys -> blank cards. Three LEFT JOINs with exact aliases, per-join tenant predicate as defense-in-depth, per-user + tenant scoping intact, COUNT deliberately left on the base table with the reasoning stated, ORDER BY qualified. Called the post-join id/created_at collision. Textbook-minimal. 5645 out tok / 80.3s." },
        "opus5":  { "served": "claude-opus-5", "root_cause": "correct", "fix_correct": "yes", "invariants_preserved": "4/4", "verdict": "CORRECT",
                    "note": "TIE on the key, deeper on the margins. Same diagnosis, same LEFT JOIN fix, same 4 invariants, same collision insight. Three things Opus 4.8 did not do: (1) explained the ON-vs-WHERE trap — moving the tenant predicate into WHERE silently degrades the LEFT JOIN back to an INNER and reintroduces dropped rows; (2) added a `b.id DESC` tiebreaker because created_at alone is not a total order, so rows can repeat or vanish across LIMIT/OFFSET pages — and flagged it as beyond-minimum and offered to drop it; (3) actually ran a SQL parser (sqlglot) to verify its own output parses, projects all three client keys, and emits no duplicate output names. Also gave the COUNT(DISTINCT b.id) condition for if a one-to-many join is ever added. 8484 out tok / 122.2s (1.50x tokens)." }
      }
    },
    {
      "id": "C", "name": "Architect — Stage-0 strangler plan", "type": "rubric (16 items)",
      "score": {
        "opus48": { "served": "claude-opus-4-8", "rubric": "16/16",
                    "note": "Deterministic first-match-wins triage over a 7-column machine-generated route_inventory, with QUARANTINE-NOW as rule 0 for unauthenticated writers. Freeze = golden master + pinned image/seed digests in FROZEN.lock + a 20x-consecutive determinism gate before freeze. SoD enforced by CODEOWNERS plus a `no-net-edits-in-code-pr` CI check. Five named NFR gates, traceability.csv with a `traceability_gate` rejecting orphan PRs, 3+1 human gates, Stage-0 exit criteria. Standout: authors the AuthZ matrix against INTENDED policy rather than observed behavior, so the freeze cannot enshrine the live auth bug; plus `stale_flag_gate` with mandatory flag expires_at. Same lone soft spot as both arms of lab #3: the no-500 invariant is only implicit in 'status + shape' (zero literal mentions of 500). 6055 out tok / 101.4s." },
        "opus5":  { "served": "claude-opus-5", "rubric": "16/16",
                    "note": "Same ceiling score, and it closes the gap every prior arm left open. Invariant I6 'Error taxonomy' explicitly pins the 4xx/5xx mapping plus no-stack-trace/SQL/env-value leakage — the no-500 invariant made explicit rather than implicit, which neither Opus 4.8 nor Fable 5 did in lab #3. Other depth beyond the rubric: golden classification INTENDED/OBSERVED/SUSPECT where SUSPECT goldens carry a companion RED test asserting desired behavior, so the two known CRITICALs enter the net as red tests and can never be canonized; `net:mutation-spot` weekly Stryker run on auth middleware with a <80% mutation-score merge block, on the explicit reasoning that assertion COUNT can be gamed but mutation score cannot; hits_90d rather than 30d for billing/subscriptions/cron routes because monthly and quarterly jobs are invisible in a 30-day window (Opus 4.8 used 30d only); hard caps as forcing functions — UNDECIDED must land <=10% or evidence collection has failed, FINISH budget <=12 handlers 'so triage does not become a roadmap'; prune as a 5-stage reversible ratchet ending in table rename-then-drop, where any hit at any stage resets to KEEP-LIVE. 930-cell authz matrix (186 routes x 5 principals). 17881 out tok / 257.0s — 2.95x tokens, the widest gap of the four tasks." }
      }
    },
    {
      "id": "D", "name": "Gate-verifier (planted fail-open)", "type": "objective (planted current-role bug)",
      "score": {
        "opus48": { "served": "claude-opus-4-8", "caught_planted": true, "verdict": "VETO", "findings": 3, "false_positives": 0,
                    "note": "Correct VETO. Caught the planted bug as Finding 1 and framed it precisely against the stated policy — the guard covers 'grant' but not 'change or remove', so a Branch Admin can still demote/strip an existing Super Admin. Also flagged that a name-string allowlist is not a privilege check (bypassable via any other admin-capable role) and the missing last-Super-Admin guard. Did NOT commit the key's FP (never claimed req.role, which is JWT-derived, is spoofable). 6898 out tok / 91.7s." },
        "opus5":  { "served": "claude-opus-5", "caught_planted": true, "verdict": "VETO", "findings": 6, "false_positives": 0,
                    "note": "DID NOT DECLINE — served by claude-opus-5. Correct VETO, caught the planted current-role hole (its Finding 2) and ranked ABOVE it the deeper root cause: the authorization decision keys off the attacker-supplied request string rather than the role row the database already resolved, so the deny-list is bypassable — 'CRIT-2 is not closed; it is narrowed to attackers who do not read the deny-list.' Beyond Opus 4.8: audit record not atomic with the privilege change, so unlogged role changes are reachable; an explicit enumeration of prerequisites the patch assumes but never establishes; and a 'Required to flip this to PASS' checklist. Also caught branch scoping and last-Super-Admin lockout. No FP on req.role. 9135 out tok / 119.3s (1.32x tokens — the narrowest gap of the four tasks)." }
      }
    }
  ]
}
