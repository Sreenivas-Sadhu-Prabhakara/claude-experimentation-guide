# Copy this file to `.env` and paste your key in.
#   cp .env.example .env
#
# Get a key at https://console.anthropic.com
# Your org must have 30-day data retention enabled (Fable is not available
# under zero-data-retention — it 400s on every request otherwise).
ANTHROPIC_API_KEY=sk-ant-your-key-here
